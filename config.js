// StratDots website config
// IMPORTANT: GitHub Pages is served over HTTPS, so this must be an HTTPS URL in production.
// Recommended: point this at a domain (e.g. https://api.yourdomain.com) that reverse-proxies to your VPS.
window.STRATDOTS_API_BASE = window.STRATDOTS_API_BASE || "https://api.stratdots.net";
